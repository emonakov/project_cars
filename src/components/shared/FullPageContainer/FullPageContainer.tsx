import styled from 'styled-components';
import ContainerUnstyled from '@material-ui/core/Container';

const FullPageContainer = styled(ContainerUnstyled)`
    && {
        max-width: 100%;
    }
`;

export default FullPageContainer;
